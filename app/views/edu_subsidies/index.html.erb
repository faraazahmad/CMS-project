<div class="container">
  <br><br><br>
  <h1 class="title is-striped is-size-1">Education subsidy subscribers</h1>

  <table>
    <thead>
      <tr>
        <th>UID</th>
        <th>Name</th>
        <th>Action</th>
      </tr>
    </thead>

    <tbody>
      <% @edu_subsidies.each do |edu_subsidy| %>
        <tr>
          <td><%= edu_subsidy.citizen_id %></td>
          <td><%= Citizen.find(edu_subsidy.citizen_id).name %></td>            
          <td>
            <%= link_to 'Remove', 
                edu_subsidy, 
                method: :delete, 
                data: { confirm: 'Are you sure?' }, 
                class: "button" %>      
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Subscription', new_edu_subsidy_path, class: "button is-link" %>
</div>